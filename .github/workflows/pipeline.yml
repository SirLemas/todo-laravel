name: Enviar alterações para branch MAIN
on:
  push:
    branches:
      - master
jobs:
  teste:
    runs-on: ubuntu-latest
    steps:
      - run: echo "iniciando testes ..."
      - uses: actions/checkout@v4
      - run: php artisan test

  deploy:
    runs-on: ubuntu-latest
    #  permissions:
    #   contents: write
    steps:
      - run: echo "Inicia deploy para produção ..."

      # - name: Configura o git
      #   run: |
      #     git config --local user.name "${{secrets.NOME}}"
      #     git config --local user.email "${{secrets.EMAIL}}"

      # - name: Baixa branch pipeline e realiza merge com a main
      # - run: git fetch
      # - run: git checkout -b pipeline
      # - run: git merge master
      # - run: git push --force origin pipeline

    
